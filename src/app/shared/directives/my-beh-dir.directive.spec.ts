import { MyBehDirDirective } from './my-beh-dir.directive';

describe('MyBehDirDirective', () => {
  it('should create an instance', () => {
    const directive = new MyBehDirDirective();
    expect(directive).toBeTruthy();
  });
});
